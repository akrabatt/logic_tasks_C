#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdlib.h>

// return 1 if array is palindrome, 0 if isn't
bool is_palindrome(const char arr[], int size);

// sheck is inboun char character
bool is_alphab(char alph);

#endif
